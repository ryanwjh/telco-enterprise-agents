# Copy to .env for local development. Loaded automatically by ADK's AgentLoader
# when running `adk run` / `adk web` from this agent's parent directory.
GOOGLE_GENAI_USE_VERTEXAI=TRUE
GOOGLE_CLOUD_PROJECT=REPLACE_WITH_DEV_PROJECT_ID
GOOGLE_CLOUD_LOCATION=global
BIGQUERY_PROJECT_ID=REPLACE_WITH_DEV_PROJECT_ID
